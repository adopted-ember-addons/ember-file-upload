<div class="window-pane {{if title 'has-title'}} {{class}}">
  <header>
    <span class="close button"></span>
    <span class="minimize button"></span>
    <span class="maximize button"></span>
    {{title}}
  </header>
  <div>{{yield this}}</div>
</div>
