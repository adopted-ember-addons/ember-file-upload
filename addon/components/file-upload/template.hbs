<input
  id={{for}}
  type="file"
  accept={{accept}}
  capture={{capture}}
  multiple={{multiple}}
  disabled={{disabled}}
  onchange={{action "change" value="target.files"}}
  hidden
  ...atributes
>

{{~yield queue~}}
