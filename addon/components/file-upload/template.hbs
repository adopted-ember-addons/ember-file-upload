<input
  id={{this.for}}
  type="file"
  accept={{@accept}}
  capture={{@capture}}
  multiple={{@multiple}}
  disabled={{@disabled}}
  onchange={{action "change" value="target.files"}}
  hidden
  ...attributes
>

{{~yield this.queue~}}
