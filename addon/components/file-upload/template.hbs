<label class='file-upload' for={{for}}>
  <input id={{for}} type="file" accept={{accept}} multiple={{multiple}} disabled={{disabled}} onchange={{action "change" value="target"}} hidden>
  {{yield queue}}
</label>